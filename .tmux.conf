# https://github.com/tmux-plugins/tpm/pull/115/files
set-environment -g PATH "/usr/local/bin:/bin:/usr/bin" 

run-shell "tmux setenv -g TMUX_VERSION $(tmux -V | cut -c 6-)"

# Use colored terminal
set -g default-terminal "xterm-256color"


# Enable mouse scrolling
set-option -g mouse on

# Mode vi
setw -g mode-keys vi

# Change Prefix to C-a
#unbind C-b
#set -g prefix C-a
#bind-key C-a send-prefix

# Copy mode overrides
#unbind ']'
bind-key 'V' copy-mode
bind-key -T copy-mode-vi 'v' send -X begin-selection
bind-key -T copy-mode-vi 'V' send -X select-line
# bind-key -T copy-mode-vi 'r' send -X rectangle-toggle
bind-key -T copy-mode-vi 'y' send -X copy-pipe-and-cancel "xclip -in -selection clipboard"

# Override default bindings
unbind '"'
unbind '%'
unbind 'c'
bind-key '"' split-window -c '#{pane_current_path}'
bind-key '%' split-window -h -c '#{pane_current_path}'
bind-key 'c' new-window -c '#{pane_current_path}'

# Use HJKL to move between panes
bind h select-pane -L
bind j select-pane -D
bind k select-pane -U
bind l select-pane -R

# Export session output to file
bind-key P command-prompt -p 'save history to filename:' -I '~/tmux.history' 'capture-pane -S -32768 ; save-buffer %1 ; delete-buffer'

# disable desconnection session
# unbind 'C-z'

# Resize panes with C-hjkl
bind-key -r -T prefix       C-k           resize-pane -U
bind-key -r -T prefix       C-j           resize-pane -D
bind-key -r -T prefix       C-h           resize-pane -L
bind-key -r -T prefix       C-l           resize-pane -R


# TPM Plugins
# Install TPM with `git clone https://github.com/tmux-plugins/tpm ~/.tmux/plugins/tpm`

# List of plugins
set -g @plugin 'tmux-plugins/tpm'
set -g @plugin 'tmux-plugins/tmux-sensible'
set -g @plugin 'tmux-plugins/tmux-open'

# Other examples:
set -g @plugin 'github_username/plugin_name'
set -g @plugin 'git@github.com:user/plugin'
# set -g @plugin 'git@bitbucket.com:user/plugin'

# Initialize TMUX plugin manager (keep this line at the very bottom of tmux.conf)
run -b '~/.tmux/plugins/tpm/tpm'

## MISC
# use zsh as default shell:
set-option -g default-shell /bin/zsh

# Show kubernetes info in tmux:
# See: https://github.com/jonmosco/kube-tmux#installing

# # Smart pane switching with awareness of Vim splits
# is_vim='echo "#{pane_current_command}" | grep -iqE "(^|\/)g?(view|n?vim?x?)(diff)?$"'
# bind -n C-h if-shell "$is_vim" "send-keys C-h" "select-pane -L"
# bind -n C-j if-shell "$is_vim" "send-keys C-j" "select-pane -D"
# bind -n C-k if-shell "$is_vim" "send-keys C-k" "select-pane -U"
# bind -n C-l if-shell "$is_vim" "send-keys C-l" "select-pane -R"
# bind -n C-\ if-shell "$is_vim" "send-keys C-\\" "select-pane -l"

# # Auto rename window based on content
# setw -g automatic-rename on
# setw -g automatic-rename-format '#{s/.*\/}// #{pane_current_command}'

# press r to reload tmux
bind r source-file ~/.tmux.conf \; display "Reloaded!"

# status bar
set -g status-bg colour235  # black
set -g status-fg white
set -g status-interval 60  # update status every minute
set -g status-left-length 90
set -g status-right-length 100
set -g status-left '#[fg=green]#H #[fg=black]• #[fg=green,bright]#(uptime | cut -d "," -f 2-)'
set -g status-right "#(/bin/bash $HOME/.tmux/kube-tmux/kube.tmux 250 red cyan)"
# set -g status-right '#[fg=green,bright] %d %b %R '
